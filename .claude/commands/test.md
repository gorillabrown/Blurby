Run the full test suite and report results.

1. Run `npm test` (vitest run)
2. If any tests fail, read the failing test file and the source file it tests
3. Fix the issue in the source code (not the test) unless the test expectation is wrong
4. Re-run tests to confirm the fix
5. Report: total tests, passed, failed, and what was fixed (if anything)
