Build the app and verify the output.

1. Run `npm run build` (vite build)
2. Check for any warnings or errors in the output
3. Verify `dist/` contains `index.html` and the JS/CSS assets
4. Report: build status, output file sizes, any warnings
5. If the build fails, read the error, fix the issue, and rebuild
