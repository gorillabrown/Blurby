# Roadmap Plan — 2026-05-25

## Finish Line (unchanged)

**TTS Quality Confidence + Reading Experience v2.** Active phase: **Reader Runtime Solidification.**

## Macro Steps to the Finish Line

1. **Finish adapter isolation** — move Focus, Flow, and Narrate behind typed mode adapters so each mode owns its own lifecycle, clock, and cursor (1C → 1D → 1E).
2. **Close the Narrate audio-truth defect** — once Narrate owns truth-sync, make the playing source's real heard position the single source of truth for both the visual cursor and re-entry seeding (NARRATE-CLOSED-LOOP-CURSOR).
3. **Activate the quality gate** — wire `npm run test:quality` into CI so TTS regressions block merges (TTS-QUAL-CI-1).
4. **Reading Experience UX polish** — library cards, command palette, space-bar mode (UX-POLISH-1), plus the small governance cleanup (GOV-HUMAN-REVIEW-1) which can run any time.

## Phase Structure

- **Phase A — Reader Runtime Solidification (active):** adapter isolation core + the post-isolation Narrate closed-loop fix.
- **Phase B — Quality Gate Activation:** CI regression wiring once reader runtime is stable.
- **Phase C — Reading Experience UX Polish:** library/command/space-bar polish after the runtime stops churning.
- **Governance lane (parallel, anytime):** GOV-HUMAN-REVIEW-1 — Lane E, no code-runtime risk.

## Conveyor Belt (confirmed — no resequence)

| Seq | Sprint | LOE | Lane | Dispatchable | Written Status (target) |
|---:|---|---|---|---|---|
| 1 | READER-ISO-1C — Focus Adapter + Passive Surface Contract | M | A (Runtime Core) | now (1B met) | Full Spec ✓ |
| 2 | READER-ISO-1D — Flow Adapter + Section-Handoff Ownership | L | A (Runtime Core) | after 1C | Full Spec ✓ |
| 3 | READER-ISO-1E — Narrate Adapter + Audio Truth-Sync | L | A (Runtime Core) | after 1D | **promote to Full Spec** |
| 4 | NARRATE-CLOSED-LOOP-CURSOR — real-audio-position truth | L | A (Runtime Core) | **blocked by 1E** | Stub (flagged buffer gap) |
| 5 | GOV-HUMAN-REVIEW-1 — deferred governance items | S | E (Governance) | now | **promote to Full Spec** |
| 6 | TTS-QUAL-CI-1 — CI regression gate wiring | S | B/E | now (deps met) | **promote to Full Spec** |
| 7 | UX-POLISH-1 — library/command/space-bar | L | C (UI) | after runtime stable | Stub |

**Sequencing rationale (dependency-first → risk-first → eat-the-frog):**
- 1C before 1D before 1E is a hard dependency chain (each adapter migration builds on the prior boundary); Focus is lowest-risk first, Narrate (highest-risk, audio-truth) last — risk-first within the chain is deliberately inverted here because the *contract* must be proven on a safe mode before the dangerous one.
- NARRATE-CLOSED-LOOP sits immediately after 1E because the scheduler surgery is only safe once the Narrate adapter owns truth-sync (Evan, 2026-05-24).
- GOV-HUMAN-REVIEW-1 has no runtime dependency and is parallel-safe (Lane E) — it can be pulled forward into any gap without disturbing the isolation chain.
- TTS-QUAL-CI-1 is dependency-clear (TEST-GREEN-1 + TTS-EVAL-3 done) but intentionally kept behind the isolation core so the gate measures a stable runtime, per Standing Rule "Broad-suite-before-CI."

## Eager-Spec Buffer Target

Replenish to **5 full specs** (Phase D):

1. READER-ISO-1C — already full
2. READER-ISO-1D — already full
3. READER-ISO-1E — **promote**
4. GOV-HUMAN-REVIEW-1 — **promote** (skips the blocked NARRATE item per the buffer edge-case rule)
5. TTS-QUAL-CI-1 — **promote**

`NARRATE-CLOSED-LOOP-CURSOR` is a **legitimate buffer gap**: a hard dependency (the Narrate adapter from 1E) blocks authoring confident edit-site detail, and the fix is investigation-heavy by nature. It is specced at the 1E close-out, not now — flagged in the phase brief.

## No-Change Decisions

- Finish line unchanged.
- Conveyor order unchanged (already dependency-correct).
- No sprints dissolved or merged this review.
