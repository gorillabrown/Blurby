## Roadmap Status — 2026-05-31

### Roadmap health
- Sprints are moving fast: the conveyor has been shipping roughly one sprint per day across the past week, and the Catalog sequence matches the roadmap's active order exactly.
- One thing is stuck: the A4 narration defect (play → pause → play restarts the book from the beginning) has now consumed three efforts — the dissolved NARRATE-CLOSED-LOOP-CURSOR, the DIAG-1 investigation, and INTENT-CURSOR-1 — and still fails its live-QA gate 0-of-3.
- Nothing is sideways in scope; the recent additions (A5-RATE-RESEED-1, the discovery-bug hotfixes) are all on-thesis for the narration-unification and bug-closure finish line.
- Pace is strong: ~10 sprints completed inside the 7-day window.
- Buffer health: the Dashboard buffer flag is unreadable (formulas wiped), so computed from the Catalog: 4 full specs queued against the target of 5 (one of the four is gated), plus 2 stubs. Above the hard floor of 3, below the 5-spec target.
- Tooling note: the sprint-queue.xlsx Dashboard tab is non-functional — KPI cells B11–B38 are empty and B7 still names the dissolved NARRATE-CLOSED-LOOP-CURSOR. All KPIs below were computed from the Catalog tab. Remediation is already queued as HYG-XLSX-DASHBOARD-RESTORE (Seq 6).
- Stragglers: two completed sprints (NARRATE-DUAL-SOURCE-DIAG-1, NARRATE-INTENT-CURSOR-1) still have their full spec blocks sitting in the Active & Remaining Sprint Skeletons section; the roadmap acknowledges this and defers archival to "the next governance sweep."

### Recently completed (since 2026-05-24)
- [NARRATE-INTENT-CURSOR-1] **The resume-anchor consume lifecycle landed but only partially fixed the bug it targeted.** (2026-05-31)
  - A1 (hard-click exact start) regression held; A4 still failed 0-of-3 because the consume fires reactively (after the resume seed already read the stale anchor) rather than preventively. 19 new tests. Sets up the next sprint to finish the job.
- [NARRATE-DUAL-SOURCE-DIAG-1] **The A4 root cause was misdiagnosed for weeks, and an instrumented replay finally pinned it down.** (2026-05-30)
  - Refuted the in-hook dual-source race; confirmed A4 is a never-cleared reader-layer resume anchor, and identified A5 as a wrong rate-change seed source. This verdict reshaped the entire remaining sprint sequence.
- [THEME-SYNC-1 / SINGLE-INSTANCE-LOCK-1 / EXT-PAIR-1] **All three discovery bugs from the 2026-05-28 sweep are now closed.** (2026-05-29)
  - Fixed a Vite circular-chunk theme bug (BUG-182), stopped duplicate app windows, and gave Chrome-extension first-time pairing a 5-minute auth window instead of 5 seconds (BUG-183).
- [TTS-QUAL-CI-1] **The TTS quality regression gate now runs automatically in CI.** (2026-05-28)
  - Kokoro v2 quality is enforced on every relevant change, plus governance tooling (scripts/recalc.py) and an XS effort tier were added.

### Coming up (next 2-4 sprints)
- [NARRATE-PAUSE-RESUME-UNIFY-1] **The next dispatch finishes the A4 fix by making the cold-start resume seed prefer the heard position over a stale anchor.** (Seq 1, full spec)
  - This is the make-or-break sprint for the pause/resume defect; its live-QA A4 gate is 3-of-3 pause/resume cycles preserving position by ear.
- [NARRATE-A5-RATE-RESEED-1] **A small, surgical fix so changing reading speed resumes from where you actually are, not from the pre-fetch head.** (Seq 2, full spec)
  - Independent of the A4 anchor work, so it can land fast and de-risk the cheap win before the larger refactor.
- [NARRATE-APPLYRATECHANGE-COLLAPSE-1] **Collapses six duplicated rate-change reseed branches into one helper and deletes the dead non-Kokoro engine code.** (Seq 3, full spec)
  - Builds on the corrected seed from A5-RATE-RESEED-1; pays down the largest accidental-complexity surface in the narration code.
- [NARRATE-SUBSCRIBER-CURSOR-1] **The heaviest sprint — removes React from the cursor's visual-highlight path to eliminate cursor lead — but only ships if a retest still shows lead.** (Seq 4, full spec, GATED)
  - Gated on an A2-by-ear retest after the earlier sprints land; may be deferred to backlog if the lead is already acceptable.

### Where we stand
- **Current phase (Reader Runtime Solidification, Stage 2 — Narration Dual-Source Unification):** the active stage is the narration unification sequence; 4 of its sprints remain (the 4 queued full specs), after which UX-POLISH-1 and HYG-XLSX-DASHBOARD-RESTORE close the phase.
- **Finish line (TTS Quality Confidence + Reading Experience v2):** ~84% complete by sprint count (37 of 44 non-dissolved sprints done); 6 sprints remain queued (4 narration full specs + 2 stubs). A precise %-by-LOE is unavailable because the Dashboard formula is wiped and LOE units are mixed (legacy points + t-shirt sizes).

### Health read
- **Watch Closely** — cadence, sync, and test health (3,044 tests) are all strong, but the A4 pause/resume defect has slipped across three sprints without passing its gate; the next sprint (PAUSE-RESUME-UNIFY-1) is the decisive one.

### Recommendations
- [NARRATE-DUAL-SOURCE-DIAG-1 / NARRATE-INTENT-CURSOR-1] **Migrate both completed sprints' inline spec blocks out of Active & Remaining Sprint Skeletons (Completed Work Summary and Catalog already reflect them as done).**

### Larger changes detected
- [BUFFER] **Run /roadmap-review to replenish the full-spec buffer back to 5 once the narration unification sequence drains — currently 4 queued (1 gated).**
- [HYG-XLSX-DASHBOARD-RESTORE] **The Dashboard tab KPIs are non-functional; the queued restore sprint should be scheduled so future status checks can read pre-computed metrics instead of recomputing from the Catalog.**
