## Roadmap Status — 2026-05-30

### Roadmap health

- Sprints are moving fast: 13 sprints completed in the last 12 days, including the Reader Runtime mode-adapter foundation (READER-ISO-1A through 1E), three discovery-bug fixes (EXT-PAIR-1, SINGLE-INSTANCE-LOCK-1, THEME-SYNC-1), the CI quality gate (TTS-QUAL-CI-1), and the dual-source diagnostic (NARRATE-DUAL-SOURCE-DIAG-1).
- Nothing is stuck. The previously gate-blocking sprint NARRATE-CLOSED-LOOP-CURSOR was dissolved 2026-05-29 after the live-QA A4 FAIL proved the half-step approach insufficient; supersede was clean and the branch is preserved at `origin/sprint/narrate-closed-loop-cursor` commit `0f1b2c8` for reference.
- One spec-shape change worth surfacing: DIAG-1's verdict (2026-05-30) refuted the in-hook dual-source race as A4's cause and identified the reader-layer never-cleared `resumeAnchor` as the real root cause; the Stage-2 conveyor reshaped accordingly (INTENT-CURSOR-1 refocused, A5-RATE-RESEED-1 added, SUBSCRIBER-CURSOR-1 gated on an A2 retest). This is course-correction, not sideways drift — the destination is sharper than the prior framing.
- Pace is strong: about one sprint per day in May, accelerated by parallel-safe lane discipline and the eager-spec buffer pattern.
- Buffer is healthy: 5 full specs queued, 2 stubs behind them. The 5 in front are the narration unification follow-on (positions 1-5); the 2 stubs are UX-POLISH-1 and HYG-XLSX-DASHBOARD-RESTORE.
- One soft straggler in the roadmap: NARRATE-DUAL-SOURCE-DIAG-1 keeps a one-line "COMPLETE" marker in the active section pending the next governance-sweep archival migration; its full close-out is already in `docs/governance/close-outs/`, and the Completed Work Summary table already has its one-line entry. Not urgent; will fold in cleanly at the next /roadmap-review.
- One sync mismatch with the Catalog tab: DIAG-1's row has `Close-Out File = None` even though `CloseOut.NARRATE-DUAL-SOURCE-DIAG-1.2026-05-30.md` exists. One-cell edit + recalc.
- Dashboard tab is computationally inert (all KPI cells return None) — that's the SRL-080 / HYG-XLSX-DASHBOARD-RESTORE territory, already specced as Seq=7 stub. Reading from Catalog directly below until that sprint lands.

### Recently completed (since 2026-05-18)

- [NARRATE-DUAL-SOURCE-DIAG-1] **The dual-source race hypothesis was REFUTED for the A4 (pause-resume restarts at book beginning) failure; the real culprit is a never-cleared reader-layer `resumeAnchor` that suppresses both cursor advance and progress-save while it's active.** The instrumented gate also CONFIRMED A5 (rate-change skip) as a separate, simpler defect in `applyRateChange` reseeding from the pre-fetch head instead of the heard floor. The Stage-2 sequence was reshaped — INTENT-CURSOR-1 refocused as the A4 fix (reader-layer anchor lifecycle), a new A5-RATE-RESEED-1 sprint inserted, SUBSCRIBER-CURSOR-1 gated on an A2 retest.
  - Verdict captured at `docs/studies/investigations/NARRATE-DUAL-SOURCE-DIAG-1.md`; close-out at `docs/governance/close-outs/CloseOut.NARRATE-DUAL-SOURCE-DIAG-1.2026-05-30.md`.
  - *Issue: this verdict supersedes the 2026-05-29 ULTRATHINK's central architectural framing; the artifact has been left in place as a record of the journey but the dispatched specs all follow the corrected diagnosis.*

- [THEME-SYNC-1] **The reader's circular Vite chunk between Settings and TTS modules is eliminated, and BUG-182 is confirmed fixed on the v1.75.1 dev build.** Five shared TTS modules were moved into the TTS chunk, breaking the `settings → tts → settings` cycle; a live smoke pass with theme cycling across all four palettes verified the fix.

- [SINGLE-INSTANCE-LOCK-1] **Repeated launcher invocations no longer spawn duplicate Blurby windows.** The Electron main process now calls `app.requestSingleInstanceLock()` with a `second-instance` focus handler — F1 from the 2026-05-27 discovery sweep is closed.

- [EXT-PAIR-1] **The Chrome extension's WebSocket pairing no longer times out on the initial auth handshake.** A new `WS_PAIRING_TIMEOUT_MS` constant (5 min) replaces the 5-second initial auth window; structured WS logging plus 8 new tests close BUG-183.

- [TTS-QUAL-CI-1] **A CI quality-regression gate now runs `npm run test:quality` automatically on every PR touching TTS surfaces, with `scripts/recalc.py` governance tooling shipped alongside.** The LOE dropdown gained an `XS` option as a small adjacent hygiene fold-in; the gate prevents Kokoro v2 quality regressions from landing silently.

- [READER-ISO-1A through 1E] **The reader-mode isolation refactor completed across five sprints, producing a typed adapter contract, an orchestrator shell, and three mode adapters (Focus, Flow, Narrate) — all with comprehensive test coverage and no runtime behavior change.** This was the load-bearing prerequisite that unblocked the narration unification work; 1E specifically gave NarrateModeAdapter ownership of audio truth-sync, which the dispatched DIAG-1 instrumentation built on top of.

### Coming up (next 2-4 sprints)

- [NARRATE-INTENT-CURSOR-1] **Formalizes the reader-layer `resumeAnchorRef` as the intent cursor with a full SET → ACTIVE → CONSUMED → CLEARED lifecycle, fixing A4 (pause-resume restarts at book beginning) at its actual root cause.** Today the anchor is SET on click and ACTIVE-skips relocate/onLoad updates, but its only clear is one explicit-selection branch — it persists stale across pause/resume and leaks into cold-start. The sprint adds the two missing lifecycle steps (consume-on-first-advance-past-anchor and clear-on-fresh-start) and un-gates progress-save when consumed.
  - *Adaptation: this is a complete spec rewrite from the original intent-cursor framing — the architectural target shifted from "in-hook intent ref" to "reader-layer anchor lifecycle" once DIAG-1 refuted the dual-source hypothesis.*

- [NARRATE-PAUSE-RESUME-UNIFY-1] **Hardens the resume seed priority chain to prefer heard-position or an explicit resume target over the stale anchor on the cold-start path, complementing the A4 fix from INTENT-CURSOR-1.** The two sprints together close the pause-resume position-preservation failure mode end-to-end.

- [NARRATE-A5-RATE-RESEED-1] **Fixes A5 (rate-change skip) at its root by seeding the Kokoro bucket-change restart from the heard floor instead of the pre-fetch head `nextGenWordIndexRef`.** DIAG-1 isolated this as a distinct in-hook seed-source defect, separable from the reader-layer A4 issue; the fix is narrow and live-QA-verifiable in isolation.

- [NARRATE-APPLYRATECHANGE-COLLAPSE-1] **Collapses the 6 per-engine reseed branches in `applyRateChange` into a single `restartGeneration(reason)` helper, deleting dormant-engine reseed code per the Kokoro-only design constraint.** Pays down maintenance burden alongside the A5 fix; the 6 → 1 collapse is grep-verifiable.

### Where we stand

- **Current phase (Reader Runtime Solidification, Stage 2):** ~58% of the stage remaining by sprint count — DIAG-1 done, 5 unification sprints + 2 stubs ahead.
  - *(Dashboard cells return None because openpyxl stripped the formulas per SRL-080; computed by hand from Catalog.)*
- **Finish line (TTS Quality Confidence + Reading Experience v2):** roughly 7 sprints remaining (the 5 unification follow-on, UX-POLISH-1, HYG-XLSX-DASHBOARD-RESTORE) plus whatever phase-end UX work the next /roadmap-review introduces. Of the four finish-line tiers, three are landed (CI quality gate ✓, discovery bugs ✓, dispatch package for unification specced ✓); tier 2 (unification complete) and tier 4 (UX polish) remain.

### Health read

- **On Track.** DIAG-1 ran on schedule, returned a decisive verdict, and the dispatch package adjusted cleanly. Buffer is healthy. No sprint has been stuck across two status windows. The May pace (~1 sprint/day) is well above any reasonable cadence requirement. The Dashboard inert-cells issue is scheduled for repair (HYG-XLSX-DASHBOARD-RESTORE) but doesn't block any active work.

### Recommendations

- [NARRATE-DUAL-SOURCE-DIAG-1] **Sync the Catalog tab's `Close-Out File` cell to point at `docs/governance/close-outs/CloseOut.NARRATE-DUAL-SOURCE-DIAG-1.2026-05-30.md`** (currently `None` despite the file existing). One-cell xlsx edit + `scripts/recalc.py` re-save; <2 min.

### Larger changes detected

- _None — DIAG-1's active-section archival deferral is explicitly captured in its inline note ("Full archival move to `docs/planning/.Archive/ROADMAP_legacy.md` is deferred to the next governance sweep"); leave as-is and let the next /roadmap-review fold it in._

---

*Inputs: ROADMAP.md (current as of 2026-05-30 update), docs/governance/sprint-queue.xlsx Catalog tab (Dashboard tab inert per SRL-080), docs/governance/close-outs/ directory listing, prior checkin docs/planning/roadmap-reviews/checkins/2026-05-18-status.md.*
