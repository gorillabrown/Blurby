# Roadmap Status — 2026-05-18

## Roadmap health

- Sprints are moving fast — three landed in the last two days, all passing verification on first attempt.
- Nothing is stuck or blocked.
- No sideways scope additions since the last roadmap review (2026-05-17).
- Pace is strong: 3 sprints shipped in 2 days (NARR-MEDIA-1, NARR-PAUSE-1, TTS-PARITY-1).
- Buffer health is running low at 3 of 5 full specs — two stubs (UX-POLISH-1, TTS-QUAL-CI-1) remain unspecced.
- No stragglers detected — active section contains only uncompleted sprints.
- Roadmap and sprint-queue.xlsx Catalog are in sync (5 queued sprints, matching order).

## Recently completed (since 2026-05-17)

- [NARR-MEDIA-1] **OS media controls — lock screen buttons, Bluetooth headphones, and media keyboards — now control narration play/pause and sentence-level track navigation.** MediaSession bridge wired through useNarration with narrationPlanner providing sentence boundaries. 52 tests across 5 files. v1.75.1.

- [NARR-PAUSE-1] **Narration pauses now carry a reason — rate change, voice change, seek, user stop, book end — and the system auto-resumes when appropriate instead of leaving the reader stuck.** Seven named pause reasons with auto-resume for rate/voice changes, seek-to-position resume, and MediaSession awareness. New namedPause.test.ts.

- [TTS-PARITY-1] **Three audit-flagged defects that undermined cache consistency, cursor accuracy, and pipeline robustness are resolved — cached audio now includes post-silence data, trusted word timing skips artificial cursor lag, and pipeline resume respects backpressure.** OutsideAudit.9 prerequisite cleared; OutsideAudit.10 (re-audit) is now unblocked. SRL-043 discovered (backpressure requires both cap gating and demand-driven drain). 123 focused tests. Merged at 67c6898.

## Coming up (next 3 sprints)

- [NARR-SPOKEN-1] **Separates the words sent to Kokoro for pronunciation from the words displayed in the reader, so punctuation-only tokens no longer produce zero-duration timestamps that force the system into heuristic fallback.** A bidirectional mapping layer reconstructs cursor positions from spoken indices back to display indices. Prerequisites met (TTS-PARITY-1 ✓).

- [NARR-CURSOR-2] **Uses real word-level end-times from Kokoro to hold the narration cursor still during silence gaps instead of smoothly gliding through dead air.** Also freezes cursor position during system-initiated pauses (rate/voice changes). Depends on NARR-SPOKEN-1.

- [TTS-EVAL-3] **Runs the existing quality evaluation harness against the full fixture corpus on current main, captures a production-grade Kokoro baseline, and wires the gate runner into a pre-merge CI check.** Parallel-safe with Stage 1a narration work.

## Where we stand

- **Current phase (Reading Experience v2):** ~63% of work remaining (3 of 8 phase sprints completed).
- **Finish line (TTS Quality + Reading Exp v2):** 5 sprints remaining. Buffer: 3/5 full specs (running low).

## Health read

- **On Track** — velocity is high (3 sprints in 2 days), all prerequisites are clearing in sequence, and the OutsideAudit.10 re-audit gate is now unblocked. Buffer running low is the only watch item — will need /roadmap-review after ~2 more sprints to replenish stubs.

## Recommendations

- _No recommendations — status quo holds._ Active section is clean, Catalog is in sync, and no stragglers need migration. Buffer replenishment (stubs → full specs) will be needed after NARR-CURSOR-2 completes but is not yet urgent.
