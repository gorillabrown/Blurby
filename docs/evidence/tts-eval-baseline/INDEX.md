# TTS-EVAL Baseline Evidence

Source root: `artifacts/tts-eval/`.

These files are retained as canonical baseline and gate records for the TTS-EVAL
matrix: aggregate summary, run summary, gate report, per-scenario summaries, and
streaming Qwen baseline summaries.

- `aggregate-summary.json` from `artifacts/tts-eval/aggregate-summary.json`
- `summary.json` from `artifacts/tts-eval/summary.json`
- `gate-report.json` from `artifacts/tts-eval/gate-report.json`
- `summaries/` from `artifacts/tts-eval/summaries/`
- `streaming-baseline/` from `artifacts/tts-eval/streaming-baseline/`
